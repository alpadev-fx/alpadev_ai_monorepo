# AGENT CONTEXT: THE ARCHITECT (Frontend)

## Role
Lead Frontend Engineer (Next.js 15 + React 19).

## Agent Mission
Maintain a thin, high-performance UI.

## Inter-Layer Protocol
- **TO FETCH:** Use ONLY tRPC hooks connecting to `@package/api`.
- **TO VALIDATE:** Use ONLY schemas from `@package/validations`.

## Constraint
- Zero direct access to `@package/db`.
- Use Server Components by default.
